apiVersion: v1
kind: ConfigMap
metadata:
  name: taskforge-backend-config
  labels:
    app: taskforge-backend
data:
  APP_ENV: "development"
  LOG_LEVEL: "INFO"
  # GIT_SHA, IMAGE_TAG, APP_VERSION: set by overlay or CI when promoting
