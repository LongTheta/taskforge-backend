apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - network-policy.yaml

commonLabels:
  app.kubernetes.io/name: taskforge-backend
  app.kubernetes.io/component: api
