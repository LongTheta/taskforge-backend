# Placeholder secret - replace with real values or external secret manager
# kubectl create secret generic taskforge-backend-secrets \
#   --from-literal=database-url='postgresql://user:pass@host:5432/db' \
#   --from-literal=secret-key='$(openssl rand -hex 32)'
apiVersion: v1
kind: Secret
metadata:
  name: taskforge-backend-secrets
  labels:
    app: taskforge-backend
type: Opaque
stringData:
  DATABASE_URL: "postgresql://taskforge:taskforge@postgres:5432/taskforge"
  SECRET_KEY: "change-me-use-external-secret"
