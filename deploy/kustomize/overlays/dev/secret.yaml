# LOCAL / DEV / DEMO ONLY — DO NOT USE IN PRODUCTION
# Placeholder secret for dev overlay. Prod uses External Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: taskforge-backend-secrets
  labels:
    app: taskforge-backend
type: Opaque
stringData:
  DATABASE_URL: "postgresql://taskforge:taskforge@postgres:5432/taskforge"
  SECRET_KEY: "change-me-use-external-secret"
